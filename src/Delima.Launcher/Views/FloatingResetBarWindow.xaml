<Window x:Class="Delima.Launcher.Views.FloatingResetBarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="DELIMa Floating Reset Bar"
        Height="54" Width="440"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        ResizeMode="NoResize">

    <Border Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="16"
            Margin="4"
            Effect="{DynamicResource CardShadow}"
            Padding="14,6">

        <Grid VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Pupil Display Information -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="28" Height="28"
                        CornerRadius="14"
                        Background="{DynamicResource SoftSurfaceBrush}"
                        BorderBrush="{DynamicResource PrimaryBrush}"
                        BorderThickness="1.5"
                        Margin="0,0,10,0">
                    <TextBlock Text="👤"
                               FontSize="14"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Border>
                <TextBlock x:Name="PupilNameText"
                           Text="Murid Aktif"
                           FontWeight="Bold"
                           FontSize="14"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"
                           MaxWidth="240" />
            </StackPanel>

            <!-- Red Logout / Reset Button (Log Keluar) -->
            <Button x:Name="LogoutButton"
                    Grid.Column="1"
                    Click="OnLogoutClicked"
                    Background="{DynamicResource AlertBrush}"
                    Foreground="#FFFFFF"
                    BorderThickness="0"
                    FontWeight="Bold"
                    FontSize="13"
                    Padding="16,6"
                    Cursor="Hand"
                    VerticalAlignment="Center">
                <Button.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="10" />
                    </Style>
                </Button.Resources>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🚪 " FontSize="12" VerticalAlignment="Center" />
                    <TextBlock Text="Log Keluar" FontSize="13" FontWeight="Bold" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </Grid>
    </Border>
</Window>
